import { clinicalRoutes } from '../routing/clinicalRoutes';
import { describe, it, expect } from 'vitest';

describe('Clinical Routes', () => {
  it('is a valid React Node', () => {
    expect(clinicalRoutes).toBeDefined();
  });
});
