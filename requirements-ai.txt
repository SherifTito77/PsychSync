# Optional AI/ML dependencies - install only if running ML inference locally
# pip install -r requirements-ai.txt
torch>=2.6.0
transformers>=4.37.0
pymc>=5.10.0
xgboost>=2.0.0
arviz>=0.18.0
statsmodels>=0.14.0
